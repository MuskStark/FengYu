{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://fengyu.dev/plugin/flow-node.schema.json",
  "title": "FengYu host flow-node declarations",
  "description": "Validates the host's FengYu/src/main/resources/flow-nodes/builtin.json — an array of the same flowNode objects plugin manifests declare under flowNodes. The definitions live in manifest.schema.json (single source of truth); this wrapper only pins the array document shape.",
  "type": "array",
  "items": {
    "$ref": "manifest.schema.json#/definitions/flowNode"
  }
}
