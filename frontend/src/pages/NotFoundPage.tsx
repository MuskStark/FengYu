/** 404 — P0 placeholder; the full surface lands with its phase batch. */
export default function NotFoundPage() {
  return (
    <div className="cx-page">
      <h1 className="cx-page-title">404</h1>
      <p className="cx-muted">404 surface ships in this phase.</p>
    </div>
  )
}
