import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs/promises';import os from'node:os';import path from'node:path';import{createPlugin}from'../src/create.mjs';import{validate}from'../src/manifest.mjs';import{writeZip}from'../src/zip.mjs';
test('create validate build',async()=>{const base=await fs.mkdtemp(path.join(os.tmpdir(),'fy-cli-')),root=path.join(base,'demo');await createPlugin(root,'com.example.demo');assert.deepEqual(await validate(root),[]);const out=path.join(base,'demo.fyp');const r=await writeZip(root,out);assert.ok(r.files>=3);const b=await fs.readFile(out);assert.equal(b.readUInt32LE(0),0x04034b50)});
