# Features

## Built-in Tools

### JSON Formatter (`DEV`)
Format, compress, and validate JSON data.

### Base64 (`DEV`)
Encode text to Base64 or decode Base64 strings.

### Hash Calculator (`DEV`)
Calculate hash digests (MD5, SHA-1, SHA-256) for text input.
