# Features

## Plugin Lifecycle

Plugins can override `onActivate()` and `onDeactivate()` to respond to
navigation events.

## Categories

Tools are organized by `ToolCategory` enum: `DEV`, `TEXT`, `IMAGE`, `NET`, `OTHER`.

## Built-in Tools

### AI Chat (DEV)
Chat with AI models. Supports local GGUF and remote APIs.
