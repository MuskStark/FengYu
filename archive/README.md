# Archive

Retired trees kept for reference only. Nothing here is built, tested, or shipped.

- `frontend-vue/` — the original Vue 3.5 + Vuetify 3 SPA, the 4.0.x frontend through
  `4.1.0-alpha.1`. Superseded by the React rewrite now living at `frontend/`
  (switchover on the `release/4.1.0` line, 2026-09). `node_modules/` and `dist/` were
  dropped on archival — run `corepack yarn install` inside the tree to revive it.
  Last state includes the uncommitted 4.1.0 coding-agent Phase 3 work (workspace
  browser REST, sidebar project grouping, @-mention inline tokens).
