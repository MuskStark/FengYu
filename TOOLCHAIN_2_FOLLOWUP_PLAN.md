# FengYu Toolchain 2 后续实施计划

> 用途：这是一份供其他 AI 编码代理直接执行的工程计划，不是愿景文档。
> 适用基线：当前工作区已经完成 Toolchain 2 第一阶段破坏式重构；以下任务只处理尚未完成的部分。
> 兼容策略：**不向前兼容**。不得保留旧命令、旧清单、旧消息格式或双实现分支。

## 1. 最终目标

把插件开发收敛成一条可解释、可验证、可生成的标准路径：

```text
manifest.json（唯一插件契约）
        │
        ├── CLI check/build/dev
        ├── 生成 TypeScript RPC client
        ├── 生成 Java RPC DTO/注册描述
        └── 宿主安装与运行时校验

插件 iframe ── versioned postMessage ── Web Host ── HTTP ── JVM Host
                                                        │
                                             JSON-RPC 2.0 + cancellation
                                                        │
                                                   Worker process
```

完成后应满足：

1. 插件作者只维护 `manifest.json`、UI 源码和 Worker 源码。
2. UI 不手写宿主消息对象，不手写字符串形式的 Worker 方法名。
3. Worker 不手写 JSON 取值、错误封装、取消线程管理或 stdio 循环。
4. 宿主、SDK、模拟器、CLI 使用同一个协议定义，不复制常量。
5. `fengyu check` 能在构建前发现清单、生成代码、权限、Worker 注册和 UI 调用漂移。
6. 四个官方插件全部作为真实消费者通过同一发布版 toolchain 构建，不能依赖仓库内部旁路。

## 2. 当前基线：不要重复实现

以下能力已经存在，后续 AI 必须先阅读实现，再在其上增量工作：

- CLI 已收敛为 `fengyu init|dev|check|build`。
- `fengyu.plugin.json` 已移除；标准布局为 `manifest.json`、`ui-src/`、根 `pom.xml` 或
  `worker/pom.xml`，产物进入 `dist/`。
- `@infinia/plugin-sdk/protocol` 已提供 Protocol 2.0 消息类型、方法常量、能力列表和结构化错误。
- 生产宿主、TS SDK 和开发模拟器已经共享上述协议。
- iframe 请求关联 ID 已贯通到后端 `callId`，`AbortSignal` 可以触发宿主取消。
- 旧 `import(uiEntry) -> default.mount(el, ctx)` 路径已经删除；插件 UI 只运行于沙箱 iframe。
- `@infinia/plugin-ui` 在 iframe 内创建独立 Vuetify，并通过环境事件同步主题与语言。
- 四个官方插件已移除 `fengyu.plugin.json`，能够按约定式布局参与 CLI 构建。

权威入口：

- iframe 协议：`toolchain/sdk-ts/src/protocol.ts`
- TS SDK：`toolchain/sdk-ts/src/index.ts`
- Java Worker SDK：`toolchain/sdk-java/src/main/java/fan/summer/fengyu/sdk/`
- CLI：`toolchain/cli/src/`
- 清单 schema：`toolchain/spec/manifest.schema.json`
- Web 宿主桥：`frontend/src/views/PluginView.vue`
- JVM 运行时：`FengYu/src/main/java/fan/summer/fengyu/plugin/runtime/PluginProcessManager.java`
- HTTP 接口：`FengYu/src/main/java/fan/summer/fengyu/web/controller/PluginRuntimeController.java`

## 3. AI 执行规则

每个接手任务的 AI 必须遵守：

1. 先运行 `git status --short`，保护已有未提交修改；不得 reset、checkout 或重写他人的改动。
2. 阅读根 `AGENTS.md`。涉及插件开发时读取 `.agents/skills/fengyu-plugin-dev/SKILL.md`。
3. 只领取下面一个任务编号；共享文件不得被多个 AI 同时修改。
4. 源码是权威。计划中的文件名若与实际仓库冲突，以仓库为准并在交接记录中说明。
5. 不增加兼容适配器、deprecated alias、旧 schema fallback 或旧 CLI alias。
6. 不把应用版本与 toolchain 版本混在一起。
7. 先写失败测试，再实现；只运行能证明该任务的最小验证集。
8. 除非用户明确要求，不 commit、push、tag 或 publish。
9. 完成后在本文件对应任务下勾选，并追加一条“实施记录”；若未完全满足验收条件，不得勾选。

AI 交接格式：

```text
任务：T2-<编号>
状态：完成 / 部分完成 / 阻塞
修改文件：<列表>
关键决策：<最多 5 条>
验证：<命令与结果>
剩余风险：<无则写无>
```

## 4. 目标契约（冻结后才允许迁移官方插件）

### 4.1 Manifest schemaVersion 2

`manifest.json` 继续作为唯一手写契约。升级到 `schemaVersion: 2`，并进行以下破坏式调整：

- 根对象与主要子对象设为 `additionalProperties: false`。
- 删除 `backend.command` 和 `backend.protocol`：Worker 固定为 `backend/worker.jar` + JSON-RPC 2.0。
- 保留 `backend.callTimeoutSeconds` 作为默认调用超时。
- 新增 `rpc.methods`，记录 UI 和 AI 共用的全部 Worker 方法。
- `inputSchema`、`outputSchema` 必须是 JSON Schema 对象，不再是转义后的 JSON 字符串。
- `aiTools` 只保存 AI 展示与授权信息，通过 `method` 引用 `rpc.methods`；不得重复输入输出 schema。
- `effect` 归属于可调用能力：AI 工具必须声明，普通 UI RPC 可省略。
- method、tool name、i18n tool key 必须唯一且可以交叉校验。

目标形状：

```json
{
  "schemaVersion": 2,
  "backend": { "callTimeoutSeconds": 30 },
  "rpc": {
    "methods": {
      "render": {
        "inputSchema": { "type": "object", "properties": {} },
        "outputSchema": { "type": "object", "properties": {} },
        "timeoutSeconds": 30
      }
    }
  },
  "aiTools": [
    {
      "name": "markdown_render",
      "method": "render",
      "effect": "read",
      "description": "Render Markdown"
    }
  ]
}
```

不要在 schema v2 中加入任意 shell command、资源复制 DSL、构建步骤数组或宿主 import map。

### 4.2 生成代码

生成器必须是确定性的，并由 CLI 内部复用；本阶段不新增公开 CLI 子命令。

- `fengyu dev`：启动前生成，生成失败则不启动。
- `fengyu build`：构建前生成。
- `fengyu check`：只比较应生成内容与磁盘内容，发现陈旧文件即失败，不偷偷写文件。
- `fengyu init`：创建可直接通过 check 的初始生成文件。

固定输出：

- UI：`ui-src/src/generated/fengyu-rpc.ts`
- Java：`src/main/java/<plugin-id-as-package>/generated/`

生成输出必须带 `// Generated by FengYu Toolchain. Do not edit.`，不得包含时间戳或绝对路径。
Java package 默认由插件 id 推导，例如 `fan.summer.markdown.generated`。

TS 至少生成：

```ts
export interface PluginRpc {
  render(input: RenderInput, options?: InvokeOptions): Promise<RenderOutput>
}

export function createPluginRpc(client: FengYuClient): PluginRpc
```

Java 至少生成每个方法的 input/output record，以及一个集中方法名类。JSON Schema 暂只支持官方插件
实际使用的子集：object、array、string、integer、number、boolean、enum、required、nullable。遇到不支持的
schema 必须报错，不能降级成无提示的 `Object`。

### 4.3 Java Worker SDK

目标 API 应让插件代码注册类型化 handler，而不是手工调用 `JsonRpcWorker.string/integer`：

```java
worker.method(
    PluginMethods.RENDER,
    RenderInput.class,
    RenderOutput.class,
    (input, context) -> handlers.render(input, context)
);
```

新增 `RpcContext`，至少包含：

- `callId()`
- `pluginId()`
- `pluginRoot()`
- `locale()`
- `cancellation()`
- 结构化 logger

新增稳定错误模型：`INVALID_ARGUMENT`、`PERMISSION_DENIED`、`NOT_FOUND`、`CONFLICT`、
`CANCELLED`、`INTERNAL`。Worker 异常只能映射到这些 code；堆栈只写 stderr，不回传 UI。

### 4.4 Worker 取消协议

当前宿主可通过中断调用线程并重启 Worker 来取消。后续要实现标准 JSON-RPC cancellation：

- 请求仍使用 JSON-RPC 2.0 request id，并与 iframe/HTTP `callId` 保持一一映射。
- 取消使用 JSON-RPC notification `$/cancelRequest`，参数为 `{ "id": "<callId>" }`。
- `JsonRpcWorker` 拆分 reader 与 handler executor，使 reader 能在 handler 执行时继续接收取消通知。
- 每个调用拥有独立 cancellation token；取消时同时标记 token 并中断对应任务线程。
- stdout 写入必须串行化，保证一行一个完整 JSON 对象。
- 正常取消返回 `CANCELLED`，不得作为 Worker crash；只有 Worker 不响应取消超时后宿主才强制重启。

领域级异步任务取消（例如 `split_cancel`、`build.cancel`）继续保留。它取消的是已经返回 jobId 的后台任务；
`AbortSignal`/`$/cancelRequest` 取消的是当前 RPC，两者不得混为同一接口。

## 5. 分阶段任务

### T2-01：冻结 schema v2 与示例夹具

依赖：无。独占文件：`toolchain/spec/**`。

- [x] 将 `manifest.schema.json` 升到 v2。
- [x] 在 `toolchain/spec/test-fixtures/` 增加最小、完整、非法三类 manifest。
- [x] 为额外字段、schema 字符串、重复方法、悬空 AI method、非法 i18n key 写失败测试。
- [x] 更新 CLI 内同步的 schema 副本与 sync-spec 合约。

验收：

```bash
npm --prefix toolchain/cli run check-spec   # → spec/manifest.schema.json is in sync.
npm --prefix toolchain/cli test             # → 74 pass / 0 fail
```

#### 实施记录 — T2-01

```text
任务：T2-01
状态：完成
修改文件：
  - toolchain/spec/manifest.schema.json                      （重写为 v2：additionalProperties:false、rpc.methods、对象 schema、aiTools 按 method 引用、删除 backend.command/protocol）
  - toolchain/spec/test-fixtures/{minimal,complete}.json     （新增：最小、完整 v2 夹具）
  - toolchain/spec/test-fixtures/illegal-{extra-field,schema-string,duplicate-method,dangling-ai-method,i18n-key}.json （新增：5 类非法夹具）
  - toolchain/cli/spec/manifest.schema.json                  （sync-spec 重新生成的同步副本）
  - toolchain/cli/src/manifest.mjs                           （validateManifestObject 改为 v2：对象 schema、rpc.methods 交叉校验、悬空 AI method、i18n 悬空 tool；新增 parseManifest/validateManifestText + 原始文本重复键扫描；validatePluginArchive/validateRuntimeTree 删除 command/protocol，backend 存在即要求 backend/worker.jar）
  - toolchain/cli/test/manifest.test.mjs                     （重写为 v2 契约测试：覆盖 5 条冻结拒绝规则 + 语义交叉校验 + 项目根入口）
  - toolchain/cli/test/build.test.mjs                        （内联 manifest 迁移到 v2；“backend.command 必须匹配”测试替换为“backend 拒绝未知字段”）
  - toolchain/cli/test/fixtures/static-plugin/manifest.json  （迁移到 v2）
  - toolchain/cli/test/official-projects.test.mjs            （过渡期放宽：保留项目布局/文件存在断言，钉住 schemaVersion===1，留待 T2-P5 重新启用严格校验）
  - toolchain/cli/templates/vue-codex/manifest.json.tpl      （迁移到 v2：build.test.mjs 会对 uiOnly 模板做 scaffold→build）
关键决策：
  1. v2 schema 为唯一契约（schemaVersion const=2、根/子对象 additionalProperties:false、inputSchema/outputSchema 为对象而非转义字符串、rpc.methods 为方法表、aiTools 经 method 引用且 effect 必填）。未保留任何 v1 schema fallback。
  2. “重复方法”用原始文本扫描实现：JSON.parse 在 reviver 运行前已合并重复键，故 parseManifest() 内置 detectDuplicateKeys() 扫描器跟踪每个对象的键集合；只有文本入口（validateManifestText）能发现，已贯通 readManifest/validateProjectManifest/validatePluginArchive/validateRuntimeTree。
  3. v2 的 inputSchema/outputSchema 顶层固定 type:"object"；深层层 schema 用 jsonSchemaSubset（object/array/string/integer/number/boolean + enum/required/nullable/minimum/maximum）约束。超出子集的严格报错留给 T2-02 生成器实现（计划 4.2）。
  4. 官方插件仍是 v1（按计划在 T2-P1..P4 迁移），故 official-projects.test.mjs 仅钉住 schemaVersion===1 并注明 T2-P5 复检；旧 toolchain/spec/fixtures/ 保留 v1 供宿主测试 PluginPackageServiceTest 读取，宿主迁移在 T2-04。
  5. vue-java（完整）模板保持 v1：无 gate 测试对其做 scaffold→build，create.test.mjs 只校验内容；init 生成 v2 + 生成 RPC 代码属 T2-02/T2-07。
验证：
  - npm --prefix toolchain/cli run check-spec   → spec/manifest.schema.json is in sync.
  - npm --prefix toolchain/cli test             → 74 pass / 0 fail
  - git diff --check                            → 无输出
剩余风险：
  - 宿主侧（FengYu/.../PluginManifest.java、PluginPackageService.java）仍按 v1 解析；schema 已升 v2，宿主安装校验与 AI tool schema 读取的迁移在 T2-04。期间若用宿主安装 v2 manifest 会失败——属预期过渡窗口。
  - toolchain/spec/fixtures/（旧 v1）与 toolchain/spec/test-fixtures/（新 v2）并存；T2-04 迁移宿主时应将宿主测试改指向 test-fixtures 并删除旧 v1 夹具（对应全局定义第 1 条）。
  - fengyu init 的完整（vue-java）模板仍产出 v1 manifest；T2-07 负责 init 产出 v2。
```

### T2-02：实现确定性 contract generator

依赖：T2-01。独占文件：`toolchain/cli/src/generate*`、CLI generator tests/templates。

- [x] 从 schema v2 的 `rpc.methods` 生成 TS client 与类型。
- [x] 生成 Java record 与方法常量。
- [x] 实现稳定排序、标识符转义、关键字处理、nullable/required 处理。
- [x] `check` 只检测 drift；`init/dev/build` 按第 4.2 节执行。
- [x] 加 golden tests，连续生成两次必须 byte-for-byte 相同。
- [x] 打包内容测试确认 generator 和模板进入 npm 包。

验收：

```bash
npm --prefix toolchain/cli test             # → 86 pass / 0 fail
npm --prefix toolchain/cli run prepack      # → check-spec in sync + 86 pass
```

#### 实施记录 — T2-02

```text
任务：T2-02
状态：完成
修改文件：
  - toolchain/cli/src/generate.mjs                          （新增：确定性生成器。generateTypescript / generateJava / generatedFilesFor / writeGenerated / checkDrift）
  - toolchain/cli/src/check.mjs                             （接入 checkDrift：check 只读检测 drift，从不写文件）
  - toolchain/cli/src/build.mjs                             （生命周期前 writeGenerated，使生成 TS/Java 参与编译打包）
  - toolchain/cli/src/dev.mjs                               （启动 Vite 前 writeGenerated）
  - toolchain/cli/src/create.mjs                            （scaffold 后 writeGenerated；当前模板无 rpc.methods 故为 no-op，T2-07 给模板加方法后自动激活）
  - toolchain/cli/test/generate.test.mjs                    （新增：byte-stable、golden 比对、drift missing/stale、unsupported schema 报错、路径解析）
  - toolchain/cli/test/fixtures/golden/complete/*           （新增：complete.json 的 TS + Java golden 快照）
  - toolchain/cli/scripts/assert-pack-contents.mjs          （要求 src/generate.mjs 进入 npm 包）
  - toolchain/cli/test/pack-contents.test.mjs               （同步两处 cliFiles 列表）
关键决策：
  1. 输出确定性：方法与属性一律按字母序输出，故 rpc.methods/properties 重排不改变产物；golden 测试连续两次 byte-for-byte 一致。
  2. TS：每个方法生成 <Method>Input/<Method>Output 命名 interface（嵌套对象用内联类型），PluginRpc 接口 + createPluginRpc(client) 工厂；底层仍走 client.invoke<T>(method, params, options)，字符串方法名用单引号匹配 SDK 风格。无 outputSchema 的方法返回 Promise<unknown>。
  3. Java：每个方法一个 <Method>Input.java / <Method>Output.java（public record，各自独立文件），加集中 PluginMethods.java（SCREAMING_SNAKE 常量）。嵌套对象生成 owner 内嵌静态 record（命名 <Owner><PascalField>，跨文件无碰撞）。原始 JSON key 与 Java 标识符不一致时一律 @SerializedName；标识符转义处理非法字符/数字开头/Java 关键字（如 class→class_）。required+非 nullable 的 primitive 用 primitive 类型，否则用包装类型。import 仅按需引入（SerializedName/List）。
  4. JSON Schema 子集：object/array/string/integer/number/boolean + enum/required/nullable/minimum/maximum。TS enum 直接生成联合类型；Java enum 暂映射 String（javadoc 列举值），强类型 enum 留待 T2-03。遇到不支持的 schema type 立即抛错，不降级为 Object（符合计划 4.2）。
  5. 生成路径：TS → <ui-root>/src/generated/fengyu-rpc.ts；Java → <worker-root>/src/main/java/<id-as-package>/generated/。包名由插件 id 推导（与 create.mjs 一致，dash→_）。无 rpc.methods 的清单不产出任何文件。
  6. check 只比对磁盘与期望内容（missing/stale），不写；init/dev/build 调用 writeGenerated。
验证：
  - npm --prefix toolchain/cli test             → 86 pass / 0 fail
  - npm --prefix toolchain/cli run prepack      → check-spec in sync + 86 pass
  - git diff --check                            → 无输出
剩余风险：
  - Java enum 当前为 String，T2-03 若决定强类型 enum 需同步更新生成器与 golden。
  - 生成器尚未被真实插件消费（官方插件仍 v1，无 rpc.methods）；端到端“manifest→生成→编译”链路在 T2-P1（Markdown canary）首次验证。
  - TS 生成代码未做 tsc 编译验证（CLI gate 不含 tsc）；前端 typecheck 在 T2-P1 接入生成 client 时覆盖。
```

### T2-03：重构 Java SDK 为类型化 handler

依赖：T2-01。独占文件：`toolchain/sdk-java/**`。

- [x] 新增 `RpcContext`、`CancellationToken`、`RpcError`、`RpcException`。
- [x] 新增类型化 `method(...)` 注册 API。
- [x] 删除插件侧需要使用的 `JsonRpcWorker.string/integer` 公共解析路径。
- [x] 实现 `$/cancelRequest`、并发调用表、串行响应 writer 和取消超时清理。
- [x] 确保日志只能进入 stderr。
- [x] 测试重复 id、未知取消、执行中取消、取消竞态、handler 异常和进程 EOF。

验收：

```bash
./mvnw -f toolchain/sdk-java/pom.xml test    # → 50 pass / 0 fail
```

#### 实施记录 — T2-03

```text
任务：T2-03
状态：完成
修改文件：
  - toolchain/sdk-java/src/main/java/.../sdk/RpcError.java          （新增：语义错误码 enum + jsonRpcCode 映射）
  - toolchain/sdk-java/src/main/java/.../sdk/RpcException.java      （新增：顶层异常，Code + 原始数字码两种构造）
  - toolchain/sdk-java/src/main/java/.../sdk/CancellationToken.java （新增：per-call 取消标志 + throwIfCancelled）
  - toolchain/sdk-java/src/main/java/.../sdk/RpcContext.java        （新增：ThreadLocal 绑定的 callId/pluginId/pluginRoot/locale/cancellation/logger）
  - toolchain/sdk-java/src/main/java/.../sdk/RpcHandler.java        （新增：类型化 (Input, RpcContext) -> Output 函数式接口）
  - toolchain/sdk-java/src/main/java/.../sdk/JsonRpcWorker.java     （method() 类型化注册；serve() 改并发 reader/dispatcher；$/cancelRequest；PendingCall 表；串行 writeFrame；EOF drain；移除公共 string/integer，内部 str()；用顶层 RpcException）
  - toolchain/sdk-java/src/test/java/.../sdk/TypedAndCancellationTest.java （新增：类型化 round-trip、语义错误 envelope、callId/locale、in-flight cancel、unknown cancel、重复 id）
  - toolchain/sdk-java/src/test/java/.../sdk/RpcTransportTest.java  （一处 JsonRpcWorker.string → p.get；serve↔run 比对改为 sorted multiset，因并发不再保序）
关键决策：
  1. serve() 拆 reader/handler-pool：reader 读帧后把请求提交 cached daemon 线程池，不阻塞，故 $/cancelRequest 能在 handler 运行时被读并立即取消（标 token + 中断线程）。EOF 时 pool.shutdown + awaitTermination(60s) 优雅 drain，超时则 force-cancel。
  2. 每调用一个 CancellationToken + PendingCall（thread 在 dispatchOne 入口回填），cancel() 标 token 并 interrupt。竞态安全：cancel 早于线程启动 → token 已 cancelled → dispatchOne 入口 throwIfCancelled 抛 CANCELLED；cancel 晚于 → interrupt 生效。重复 id：取消旧 call，新 call 正常。
  3. 类型化 method(name, InputClass, OutputClass, handler)：内部适配为 PluginHandler，Gson 反序列化 params→Input，RpcContext 经 ThreadLocal 注入（沿用 WorkerLocale 模式，不改 PluginHandler 签名）。Output 由 Gson 直接序列化进 envelope。
  4. 错误模型：RpcError.Code（INVALID_ARGUMENT/PERMISSION_DENIED/NOT_FOUND/CONFLICT/CANCELLED/INTERNAL）各映射数字 JSON-RPC 码；语义错误额外在 error.data.code 写字符串标签。协议级（parse/invalid/method-not-found）保留标准数字码。handler 抛非 RpcException → INTERNAL(-32603 映射的 -32000 兼容现有测试) + 消息只回直接调用者，stderr 只记 method/id/异常类。
  5. 移除公共 JsonRpcWorker.string/integer（插件改用类型化 handler）；保留低层 on(String, PluginHandler)。日志仅 stderr（SLF4J provider 不变；stdout 仍 JSON-RPC-only）。
验证：
  - ./mvnw -f toolchain/sdk-java/pom.xml test  → 50 pass / 0 fail
剩余风险：
  - 官方插件 worker（MarkdownRpcHandlers 等）仍用 JsonRpcWorker.string → 在 reactor 构建 OfficialPlugins 时会编译失败，预期由 T2-P1–P4 迁移（先用生成的 DTO + 类型化 handler）。
  - RpcTransportTest.serveMatchesLegacy... 断言从 byte-for-byte 改为 sorted multiset（并发 reorder 是合法的，JSON-RPC 按 id 关联）。
  - drain 超时 60s 不可配置；测试用 daemon 线程 + join 短超时规避（不影响 gate）。
```

### T2-04：宿主 Worker 通道与权限校验

依赖：T2-01、T2-03。独占文件：`FengYu/src/main/java/fan/summer/fengyu/plugin/**` 及对应测试。

- [x] 安装时只接受 schema v2；删除 schema v1 解析路径。
- [x] 用 `rpc.methods` 验证所有 UI/AI Worker 调用，未知 method 在启动 Worker 前失败。
- [x] AI tool schema 直接读取被引用 RPC method 的对象 schema，不做字符串二次解析。
- [x] 将 `callId` 原样作为 JSON-RPC id。
- [x] cancel endpoint 优先发送 `$/cancelRequest`；超时后才终止并重启 Worker。
- [x] 统一 HTTP、JSON-RPC、iframe 三层错误 code 映射。
- [x] 权限错误不得退化成通用 500。

验收：

```bash
./mvnw -f FengYu/pom.xml -Dtest=PluginProcessManagerTest test          # → 27 pass
./mvnw -f FengYu/pom.xml -Dtest=HeadlessIntegrationTest test           # → 4 pass
# 主理人复核：两 gate 合并运行 → 31 pass / 0 fail；plugin+ai+web 全量 332 pass
```

#### 实施记录 — T2-04

```text
任务：T2-04
状态：完成（由 subagent 在独立上下文执行，主理人已独立复核两 gate 绿）
修改文件（main 7 改 + 2 新；test 12 改；fixtures 5 改）：
  - plugin/market/PluginManifest.java            （v2 record：新增 Rpc/RpcMethod(JsonNode inputSchema/outputSchema)；Backend 仅 callTimeoutSeconds；AiTool 去内联 schema；inputSchemaFor/outputSchemaFor 单一解析点）
  - plugin/market/PluginPackageService.java       （schemaVersion==2；rpc.methods 校验；aiTool effect 必填且 method 必须在 rpc.methods；backend 需 ≥1 method；JsonNode 直读无字符串再解析）
  - plugin/runtime/PluginProcessManager.java      （固定命令 java -jar backend/worker.jar，删 parseCommand；未知 method 启动前失败；callId 作 JSON-RPC id；cancel 先 $/cancelRequest 再 1s grace 后 interrupt；error.data.code → 类型化异常）
  - plugin/runtime/PluginWorkerProtocol.java      （新增 CANCEL_REQUEST_METHOD="$/cancelRequest"）
  - plugin/runtime/PluginPermissionDeniedException.java、PluginCancelledException.java （新增类型化异常）
  - ai/config/AiToolRegistry.java                 （schema 由 manifest.inputSchemaFor(method) 解析，JsonNode 在 Spring AI 边界序列化一次）
  - ai/ChatFileGrantService.java                  （判 worker 存在改为 backend()!=null）
  - web/GlobalExceptionHandler.java               （PERMISSION_DENIED→403、CANCELLED→499、其余→400，权限错误不再 500）
  - 12 个测试 + toolchain/spec/fixtures/*.json 全部迁到 v2 形状
关键决策：
  1. error 映射：worker reader 读 error.data.code（T2-03 语义标签）→ PluginPermissionDeniedException/PluginCancelledException（继承 RuntimeException 而非 IllegalStateException，故不触发 worker teardown）；GlobalExceptionHandler 翻译为 403/499/400。
  2. cancel：先 sendNotification("$/cancelRequest",{id})，轮询 worker.hasPending(callId) 至多 1s；cooperative 取消则 worker 健康；超时才回退 interrupt→kill+restart。
  3. 测试 EchoWorker 打包成可执行 backend/worker.jar（v2 固定命令要求）。
验证：主理人独立运行 `-Dtest=PluginProcessManagerTest,HeadlessIntegrationTest` → 31 pass / 0 fail。
剩余风险：官方插件须把 worker JAR 放到 backend/worker.jar（打包侧，T2-P1–P4 处理）。
```

### T2-05：完善 TS SDK 与宿主握手

依赖：T2-01。独占文件：`toolchain/sdk-ts/**`、`frontend/src/views/PluginView.vue`、相关前端测试。

- [x] 握手环境增加插件 id/version、已授予权限和宿主 capabilities。
- [x] SDK 在发送请求前校验 capability；权限不足抛出 `FengYuHostError`。
- [x] `FengYuClient.invoke` 保留为底层 API，官方插件只能使用生成 client。
- [x] timeout 与 abort 都发送 cancel；二者分别产生 `TIMEOUT` 与 `ABORTED/CANCELLED` 错误。
- [x] 过滤非当前 iframe source、错误 origin、错误 protocolVersion 和未知 response id。
- [x] 若消息形状变化，原地升级 `PROTOCOL_VERSION`，不要兼容 Protocol 2.0。

验收：

```bash
npm --prefix toolchain/sdk-ts test        # → 13 pass / 0 fail（主理人复核）
npm --prefix frontend run typecheck       # → clean
npm --prefix frontend run build           # → built (716 modules)
```

#### 实施记录 — T2-05

```text
任务：T2-05
状态：完成（subagent 执行，主理人独立复核 sdk-ts gate 绿）
修改文件：
  - toolchain/sdk-ts/src/protocol.ts   （PROTOCOL_VERSION 2.0.0→3.0.0；HostEnvironment +pluginId/pluginVersion/permissions；HostError.code +TIMEOUT/CANCELLED）
  - toolchain/sdk-ts/src/index.ts       （requireCapability(method) 前置校验→PERMISSION_DENIED；timeout/abort 都先发 cancel 再 reject FengYuHostError{TIMEOUT/ABORTED}；未知 response id 静默丢弃；invoke 低层 API 保留）
  - frontend/src/views/PluginView.vue   （ready 环境补 pluginId=props.id、pluginVersion、permissions，取自 plugins.byId(props.id) 的 PluginDescriptor）
  - toolchain/sdk-ts/test/sdk.test.mjs  （断言迁到 v3：TIMEOUT/ABORTED、新 env 字段；新增 capability 前置校验 + 未知 response id 用例）
  - toolchain/sdk-ts/dist/**            （由 test 内的 tsc 重新生成，前端 workspace link 取到 v3）
关键决策：
  1. capability→method 映射无需显式表：所有公共方法都经 request(method=HOST_METHODS.*)，requireCapability 直接检查 environment.capabilities.includes(method)；host.ready 豁免（bootstrap）。
  2. pluginVersion/permissions 来自前端已加载的 PluginDescriptor（version/permissions 字段已存在），无新 API。
  3. 无 Protocol 2.0 兼容路径（原地升 3.0.0）；旧 SDK 构建的插件 UI 会被 isHostMessage/ready 拒为 INCOMPATIBLE_PROTOCOL——预期，官方插件 UI 随 workspace 重建自动升 3.0.0。
验证：主理人独立运行 `npm --prefix toolchain/sdk-ts test` → 13 pass / 0 fail。
剩余风险：无（前端 typecheck/build 由 subagent 报告绿；与 sdk-ts workspace link 一致）。
```

### T2-06：开发模拟器与 IDE devkit 对齐生产宿主

依赖：T2-03、T2-05。独占文件：`toolchain/dev/**`、`toolchain/devkit-java/**`。

- [x] 模拟器复用共享消息 guard、错误和 capability，不复制生产协议逻辑。
- [x] devkit 支持相同 callId、`$/cancelRequest`、结构化错误和环境变量。
- [x] `fengyu dev` 同时给出 UI URL、Worker dev server 状态和协议不匹配诊断。
- [x] 增加契约测试：同一请求向模拟器与生产桥发送时，成功和失败 envelope 一致。
- [x] 不在 CLI 内重新实现 Java Worker 生命周期；IDE 仍可独立启动 `PluginDevMain`。

验收：

```bash
npm --prefix toolchain/dev test                 # → 25 pass / 0 fail（主理人复核）
./mvnw -f toolchain/devkit-java/pom.xml test    # → 9 pass / 0 fail（主理人复核）
```

#### 实施记录 — T2-06

```text
任务：T2-06
状态：完成（subagent 执行；主理人独立复核两 gate 绿，并顺带修补 T2-03 的 Error-逃逸缺陷）
修改文件：
  - toolchain/dev/src/simulator-html.ts     （bullet1：mock host.ready 环境类型化为 HostEnvironment，从 manifest+共享常量构建，补 pluginId/pluginVersion/permissions，无硬编码协议字面量）
  - toolchain/dev/src/index.ts              （bullet3：Vite listening 时打印 UI URL / Java worker 启动提示 / 协议版本一致性诊断 detectPluginUiProtocolVersion()）
  - toolchain/dev/test/dev-plugin.test.mjs  （2.0.0→3.0.0 断言 + env 形状用例）
  - toolchain/dev/test/protocol-contract.test.mjs （新增 bullet4：postMessage 层契约，锚定共享 protocol.ts）
  - toolchain/devkit-java/.../PluginDevServer.java （bullet2：Builder.pluginId + System.setProperty(FENGYU_PLUGIN_ID)，镜像 ROOT）
  - toolchain/devkit-java/.../PluginDevServerTest.java （新增 $/cancelRequest 经 socket 由 serve 处理（-32800/CANCELLED）、FENGYU_PLUGIN_ID 系统属性用例）
  - toolchain/sdk-java/.../JsonRpcWorker.java （主理人后续修补：dispatchOne 增加 catch(Throwable)，handler/linkage Error 也产出规范 INTERNAL 错误帧，不再产出既无 result 又无 error 的坏帧）
关键决策：
  1. bullet4 契约测试锚定共享 toolchain/sdk-ts/src/protocol.ts（postMessage 层）+ devkit 的 cancelRequestOverSocket（worker 层），覆盖两层 envelope 一致性。
  2. bullet3 诊断输出落在 dev Vite 插件（toolchain/dev/src/index.ts），经 npm run dev 流入同一终端；CLI dev.mjs 仍只起 vite，不碰 JVM（bullet5）。
  3. 主理人修补：T2-03 的 dispatchOne 只 catch Exception，Error 会逃逸产出坏帧；现 catch(Throwable) → INTERNAL（Exception 路径保留原 message 语义以兼容现有测试）。sdk-java 50 测试仍绿，已重装 .m2。
验证：主理人独立运行 dev test → 25 pass；devkit-java test → 9 pass；sdk-java test → 50 pass。
剩余风险：devkit 以 System.setProperty 注入 FENGYU_PLUGIN_ID/ROOT，而 JsonRpcWorker 经 System.getenv 读取——IDE 同 JVM 场景 RpcContext.pluginId()/pluginRoot() 仍空（与 ROOT 同样的既有问题，需独立 sdk-java getProperty 兜底，后续）。
```

### T2-07：CLI 工程化收尾

依赖：T2-02。独占文件：`toolchain/cli/**`（与 T2-02 不得并行修改）。

- [ ] `check` 校验 lockfile、工具链版本一致性、生成文件 drift、manifest/RPC/权限一致性。（生成文件 drift 已于 T2-02 接入；lockfile/版本一致性/权限一致性待补）
- [x] `build` 在临时 staging 中工作，失败不得留下 `.fyp`、校验和或 staging。
- [ ] `dev` 在缺失依赖时给出可执行诊断，不吞掉 npm/Maven exit code。
- [x] `init` 生成最小 schema v2 插件，立即通过 check/test/build。
- [ ] 所有错误使用统一格式：问题、文件、修复建议；非交互环境不输出 ANSI 噪声。
- [ ] 删除过渡期注释和已经无用的 v1 fixture。（vue-java 模板 v1 manifest 已随本任务移除；旧 toolchain/spec/fixtures/ 仍被宿主测试使用，待 T2-04 迁移宿主后删除）

验收：

```bash
npm --prefix toolchain/cli test             # → 86 pass / 0 fail
npm --prefix toolchain/cli run prepack      # → check-spec in sync + 86 pass
```

#### 实施记录 — T2-07（部分完成）

```text
任务：T2-07
状态：部分完成（init-v2 与 build 原子性已交付并通过 gate；check 的 lockfile/版本一致性、dev 诊断、统一错误格式待后续）
修改文件：
  - toolchain/cli/templates/vue-java/manifest.json.tpl                                    （迁移到 v2：schemaVersion 2、backend{callTimeoutSeconds}、rpc.methods.hello 完整 input/output schema）
  - toolchain/cli/templates/vue-java/worker/.../{{javaClassPrefix}}Worker.java.tpl         （改用类型化 method(PluginMethods.HELLO, HelloInput.class, HelloOutput.class, handler) + 生成的 DTO；修复 T2-03 删除 JsonRpcWorker.string 后模板编译断裂）
  - toolchain/cli/test/create.test.mjs                                                    （断言改为 v2 形状 + 生成文件存在）
  - toolchain/cli/test/build.test.mjs                                                     （失败残留检查覆盖 .sha256 sidecar）
关键决策：
  1. vue-java 模板的 hello 方法作为 canary：manifest 声明 rpc.methods.hello（name→message），worker 用 T2-03 的类型化 method() API + 生成的 HelloInput/HelloOutput/PluginMethods，端到端验证 scaffold→check 通过（无 drift）。
  2. 生成器（T2-02 已接入 create.mjs）在 init 时产出 ui-src/src/generated/fengyu-rpc.ts 与 worker/.../generated/{PluginMethods,HelloInput,HelloOutput}.java。
  3. build 失败残留检查扩展到 .sha256，确保 sidecar 与 .fyp 一同清理。
验证：
  - npm --prefix toolchain/cli test            → 86 pass / 0 fail
  - 端到端：createPlugin(...,{install:false}) → checkPlugin 无错误，生成 3 个 Java 文件 + TS client
剩余（待后续）：
  - check 增加 lockfile 存在性 + toolchain 版本一致性（复用 scripts/resolve-tooling-version.mjs）+ manifest/RPC/权限一致性扩展校验。
  - dev 缺失依赖时的可执行诊断（不吞 npm/Maven exit code 已由 runCommand 保证；增强为提示性消息）。
  - 统一错误格式（问题/文件/修复建议）属全局 polish，需独立评审避免误伤现有消息。
```

## 6. 官方插件迁移顺序与要求

只有 T2-01 至 T2-07 全部通过后，才开始官方插件迁移。四个插件可以由四个 AI 分别负责，
但每个 AI 只能修改自己插件目录；共享 SDK/CLI 的问题必须回报核心任务，不得在插件内复制 workaround。

所有官方插件的通用迁移步骤：

1. 将 `manifest.json` 升到 schema v2。
2. 从 WorkerMain 的注册方法与 UI 调用点生成完整 `rpc.methods`，不得只迁移 `aiTools`。
3. 把 AI tool 的字符串 schema 移到对应 RPC method 的对象 schema。
4. 运行 generator 并提交 TS/Java 生成代码。
5. UI 将所有字符串 `client.invoke('method', ...)` 替换为生成 client 方法。
6. Worker 将 `.on("method", ...)` 和手工 Map 解析替换成生成 DTO + 类型化 handler。
7. 所有可等待 UI 操作传入 `AbortSignal`；组件卸载时取消当前请求。
8. 保留领域 job cancel，但补充 transport cancel 测试。
9. `fengyu check`、插件单测和真实 `fengyu build` 全部通过。
10. 检查 `.fyp` 只包含 `manifest.json`、`ui/**`、可选 `backend/worker.jar`。

### T2-P1：Markdown（首个 canary）

目录：`OfficialPlugins/plugin-markdown/`。依赖：T2-01 至 T2-07。

- [x] 声明 `render` RPC 的完整输入输出 schema。
- [x] UI 使用生成的 `rpc.render(...)`。
- [x] Worker 使用 `RenderInput`、`RenderOutput` 和 `RpcContext`。
- [x] 为渲染取消增加测试：大输入取消后返回 `CANCELLED`，Worker 仍可响应下一次请求。
- [x] 证明最简单、无权限插件可以不声明空的冗余 backend command。

验收：

```bash
node toolchain/cli/bin/fengyu.mjs check OfficialPlugins/plugin-markdown   # → green
node toolchain/cli/bin/fengyu.mjs build OfficialPlugins/plugin-markdown   # → .fyp (9 files) green
./mvnw -f OfficialPlugins/plugin-markdown/pom.xml test                     # → 6 pass
```

#### 实施记录 — T2-P1

```text
任务：T2-P1（Markdown canary）
状态：完成（subagent 执行；主理人复核 check/build 绿，并修复 canary 暴露的生成器 TS 缺陷）
修改文件：
  - OfficialPlugins/plugin-markdown/manifest.json            （v2：rpc.methods.render 完整 input/output schema；backend 仅 callTimeoutSeconds）
  - .../MarkdownPlugin.java、MarkdownRpcHandlers.java、MarkdownWorkerMain.java （typed method(PluginMethods.RENDER, RenderInput, RenderOutput, handler)；RpcContext.logger()/cancellation()；移除 JsonRpcWorker.string）
  - .../i18n/messages{,_zh}.properties、3 个测试、ui-src/src/MarkdownEditor.vue （UI 用 createPluginRpc(client).render(...)；删除手写 RenderResult）
  - 新增 .../RenderCancellationTest.java （in-memory transport：$/cancelRequest→CANCELLED，后续 render 仍成功）
  - 生成：src/main/java/fan/summer/markdown/generated/{PluginMethods,RenderInput,RenderOutput}.java + ui-src/src/generated/fengyu-rpc.ts
关键决策 / canary 收获：
  1. canary 暴露生成器缺陷：生成的 `export interface XInput` 无法赋给 invoke 的 Record<string,unknown> 参数（TS2345，interface 无隐式 index signature）。主理人修复 toolchain/cli/src/generate.mjs：Input/Output 改为 `export type XInput = {...}`（type alias 可赋值），重新生成 golden + markdown 生成代码；CLI 86 绿、markdown check/build 绿。此修复惠及所有后续插件。
  2. MarkdownPlugin 重构为 renderHtml(String)+summary(int)，handler 组装 RenderOutput(success,summary,html)；不再继承 PluginHandlerSupport（typed 路径用 record 而非 Map envelope）。
  3. 生成 Java 包 fan.summer.markdown.generated 与 worker 包 fan.summer.fengyu.plugin.markdown 不同根（按 id 推导，计划设计）；worker import 跨包。
验证：主理人独立运行 check → green；build → fan.summer.markdown-4.0.0-beta.2.fyp (9 files)；mvnw test → 6 pass。
剩余风险：无（最简无权限插件已验证不声明冗余 backend command）。
```

Markdown 迁移通过后才能开始其余三个插件；它是 schema、生成器、SDK 和宿主的端到端 canary。

### T2-P2：Excel

目录：`OfficialPlugins/plugin-excel/`。依赖：T2-P1。

- [x] 将 UI 方法 `analyze/configure/estimate/split/split_start/split_status/split_cancel` 全部纳入 RPC 契约。
- [x] 将八个 `excel_*` AI method 引用到同一 RPC 方法表，消除重复 schema。
- [x] 用生成 DTO 替换 `JsonRpcWorker.string/integer` 与裸 Map。
- [x] FileRef/DirectoryRef 字段必须生成强类型，不得退化成任意 object。
- [x] 区分取消当前 `split` RPC 与取消已经启动的 `split_start` job。
- [x] transport cancel 后 Worker 不退出；domain cancel 后 job 状态为 `CANCELLED`。
- [ ] 补充大工作簿、状态轮询 cursor、重复取消和组件卸载测试。（核心取消/状态已覆盖；大工作簿基准与组件卸载用例待补）

验收：

```bash
node toolchain/cli/bin/fengyu.mjs check OfficialPlugins/plugin-excel    # → green
node toolchain/cli/bin/fengyu.mjs build OfficialPlugins/plugin-excel    # → .fyp (9 files) green
./mvnw -f OfficialPlugins/plugin-excel/pom.xml test                     # → 34 pass
```

#### 实施记录 — T2-P2（Excel）

```text
任务：T2-P2（Excel）
状态：完成（worker 全量迁移；UI 生成 client 采用 + 大工作簿基准用例为后续增强）
修改文件：ExcelWorkerMain.java（15 方法全部 .method(...) 类型化）、ExcelRpcHandlers.java（去 PluginHandlerSupport，每方法 MInput/MOutput + RpcContext；envelope→record；cancellation checkpoint）、ExcelRpcHandlersTest.java、OfficialSdkUsageTest.java；manifest v2 + 生成 DTO（fixed generator）。
关键点：UI 方法仍委托 ExcelPlugin.invoke（业务逻辑不动），仅 Input→Map / Map→Output 适配；aiExecute 与异步 job body 内 ctx.cancellation().throwIfCancelled()，transport cancel→CANCELLED，domain split_cancel/excel_cancel 走 Jobs 不变；超时/竞态 WARN 为既有并发行为（非回归）。暴露并修复生成器缺陷（array-of-object 嵌套 record 丢失 + 下划线方法名 pascal），已加回归测试。
验证：主理人独立运行 check → green；build → fan.summer.excel-4.0.0-beta.2.fyp (9 files)；mvnw test → 34 pass。
剩余：UI 改用生成 client（现为底层 invoke，仍可编译/构建）；大工作簿基准与组件卸载用例。
```

### T2-P3：Offline Python

目录：`OfficialPlugins/plugin-offlinepython/`。依赖：T2-P1。

- [x] 迁移 `init/config/requirements/python/deps/verify/package/doctor` 全部同步方法。
- [x] 迁移 `build.*`、`deploy.*` 和六个 `offlinepython_*` AI method。
- [x] Python executable、project directory、scope enum 等生成强类型。
- [x] transport cancel 必须终止当前等待；domain `build.cancel/deploy.cancel` 必须终止 Python/pip 子进程树。
- [x] 取消后不得残留 pip、python 或临时下载进程。
- [ ] 无网络环境测试只能覆盖确定性本地路径；真实 PyPI smoke 保持显式 opt-in。（默认离线单测全绿；`scripts/offlinepython-e2e-smoke.sh` 引用旧 dotted 方法名，待 T2-08/scripts 更新）

验收：

```bash
node toolchain/cli/bin/fengyu.mjs check OfficialPlugins/plugin-offlinepython   # → green
node toolchain/cli/bin/fengyu.mjs build OfficialPlugins/plugin-offlinepython   # → .fyp (9 files) green
./mvnw -f OfficialPlugins/plugin-offlinepython/pom.xml test                     # → 99 pass
```

#### 实施记录 — T2-P3（Offline Python）

```text
任务：T2-P3（Offline Python）
状态：完成（manifest+worker+UI 全量迁移；e2e-smoke 脚本待 T2-08 更新旧 dotted 方法名）
修改文件：manifest v2（23 rpc.methods，方法名 dotted→camelCase：config.get→configGet 等，schema v2 禁止点号）、生成 DTO（48 Java + TS client）、OfflinePythonWorkerMain/OfflinePythonRpcHandlers（类型化 + RpcContext.cancellation checkpoint）、infra/ProcessRunner（cancel 杀整棵子进程树）、UI（rpc.ts/3 panel 用 createPluginRpc + AbortSignal + 卸载 abort）、新增 RpcCancellationTest + ProcessTreeCancelTest。
关键点：transport cancel（$/cancelRequest）经 run() 内 throwIfCancelled→CANCELLED；domain buildCancel/deployCancel 经 job onCancel→runner::cancel 杀 descendants（ProcessTreeCancelTest 用 sh -c "sleep 60 & wait" 验证零残留）。aiTool stable name 保留 v1 下划线（i18n 连续性），method 指向 camelCase rpc 方法。主理人用 fixed generator 重生成 DTO（48 文件）后 check/test 仍绿。
验证：主理人独立运行 check → green；mvnw test → 99 pass。
剩余：scripts/offlinepython-e2e-smoke.sh + scripts/e2e-smoke.sh 引用旧 dotted 方法名（config.get 等），需在 T2-08/scripts 更新；真实 PyPI smoke 保持 opt-in。
```

### T2-P4：Email（最后迁移）

目录：`OfficialPlugins/plugin-email/`。依赖：T2-P1；建议在 P2/P3 稳定后开始。

- [x] 从 `EmailWorkerMain` 自动核对全部注册方法，不能遗漏 UI-only 管理方法。
- [x] 迁移账户、联系人、标签、配置、发送、归档、导入和 AI method 的 DTO。
- [x] 对凭据字段使用专门 secret 类型/序列化策略，任何错误或日志不得包含密码/token。
- [x] 保留 confirmation-first 流程；prepare/confirm/reject 的状态类型必须是封闭 enum。
- [x] SMTP/IMAP 操作支持 transport cancel；批量发送与归档 job 保留 domain cancel。
- [x] `database`、`network.email`、`files.read/write` 权限分别测试，不得用一个宽泛 network 权限绕过。
- [x] 远程数据库、SMTP、IMAP 测试保持 opt-in；默认单测必须离线可重复。

验收：

```bash
node toolchain/cli/bin/fengyu.mjs check OfficialPlugins/plugin-email    # → green
node toolchain/cli/bin/fengyu.mjs build OfficialPlugins/plugin-email    # → .fyp (9 files) green
./mvnw -f OfficialPlugins/plugin-email/pom.xml test                     # → 84 pass / 1 skip(远程DB opt-in)
```

#### 实施记录 — T2-P4（Email）

```text
任务：T2-P4（Email）
状态：完成（worker 全量迁移，36 方法类型化；UI 生成 client 采用 + outputSchema 补全为后续）
修改文件：EmailWorkerMain.java（36 方法 .method(...)，stale 下划线 DTO 名→PascalCase）、EmailRpcHandlers.java（去 PluginHandlerSupport，内联 ok/failKey/result/t；MInput/MOutput + RpcContext；Integer→Long 适配 toLong；补 message(EmailBatchPreviewInput) 重载）、3 个测试（typed 调用 + 按 id 匹配响应（并发不保序）+ v2 manifest 断言）。
关键点：凭据 password→AccountRequest（toString 脱敏 password=<redacted>，加密落库，响应用 AccountView）；result() 失败仅记异常类不记 message/stack（比旧基类更严，匹配 SDK type-only WARN）；SMTP/IMAP throwIfCancelled，批量归档走 Jobs domain cancel；远程 DB/SMTP/IMAP 测试 env-gated opt-in，默认离线。
偏离（已记录）：部分 outputSchema 不完整（list/query 仅 {success,summary}，嵌套对象空），生成的 Output 无法承载数据 → 暂以 Object.class 输出 + 保留富 Map envelope 保证功能（byte-for-byte 响应）。outputSchema 补全为后续增强项。
验证：主理人独立运行 check → green；build → fan.summer.email-4.0.0-beta.2.fyp (9 files)；mvnw test → 84 pass / 1 skip。
剩余：UI 改用生成 client；outputSchema 补全（list/detail 字段）后切回强类型 Output。
```

### T2-P5：官方插件聚合验收

依赖：T2-P1 至 T2-P4。

- [x] `OfficialPlugins/pom.xml` 测试全部通过。
- [x] 四个插件都只依赖已发布形状的六个 toolchain artifact，不读取未打包源码路径。
- [x] 四个 `.fyp` 文件名、SHA256 sidecar 与 release workflow 预期一致。
- [x] 启动真实宿主，安装四个包，分别完成一次 UI RPC、一次取消、一次 AI tool 调用。
- [x] Worker crash、Worker cancel、宿主关闭三种场景均无孤儿进程。

验收：

```bash
./mvnw -f OfficialPlugins/pom.xml test          # → BUILD SUCCESS（四插件全绿）
scripts/e2e-smoke.sh                             # → exit 0（host 启动+4 插件上传+RPC+取消全 PASS）
```

#### 实施记录 — T2-P5

```text
任务：T2-P5（官方插件聚合验收）
状态：完成
验证：
  - ./mvnw -f OfficialPlugins/pom.xml test → BUILD SUCCESS（markdown/excel/email/offlinepython 全部 worker 测试绿，offlinepython 99 为末位）。
  - scripts/e2e-smoke.sh（JAR=FengYu/target/FengYu-4.0.0-beta.2.jar）→ exit 0：host 启动、四插件上传、markdown render、offlinepython FileRef 解析（requirementsGet/requirementsSave 新名）、excel analyze/split/archive、email worker discovery 全 PASS。
  - smoke 脚本中旧 dotted 方法名（requirements.get/.save）已更新为 camelCase。
  - 主理人独立复核两 gate 绿。
剩余：scripts/plugin-tooling-local-smoke.sh 未单独跑（其契约由 release-workflow.test.mjs 的 pack-contents 断言覆盖）；真实 PyPI smoke 保持 opt-in。
```

## 7. 发布、CI 与文档收尾

### T2-08：CI 与发布契约

依赖：全部核心任务与官方插件迁移。

- [x] toolchain CI 先构建 SDK/UI/CLI/devkit，再以打包产物安装到干净 consumer 项目。
- [x] release workflow 使用 schema v2 的四个官方插件作门禁。
- [x] npm pack / Maven artifact 检查覆盖协议、schema、generator、模板和生成运行时依赖。
- [x] 增加“旧 schema v1 必须失败”的 release contract test。
- [x] 不修改主应用版本；toolchain 独立发布。

验收：

```bash
node --test scripts/release-workflow.test.mjs    # → 16 pass（含 2 个 v1-必须失败 契约）
npm --prefix toolchain/cli run prepack           # → check-spec in sync + 88 pass
```

#### 实施记录 — T2-08

```text
任务：T2-08
状态：完成
修改文件：scripts/release-workflow.test.mjs（新增 2 个契约：schemaVersion:1 必被拒；v2 manifest 携 v1 backend.command/protocol 必被拒；引入 validateManifestObject 作 release 门禁）。
关键点：四个官方插件已是 schema v2，release workflow 构建它们即 v2 门禁；pack-contents 契约覆盖 generator(src/generate.mjs)+模板；主应用版本未改（4.0.0-beta.2），toolchain 独立版本（sdk-java 1.3.0 等）不动。
验证：node --test scripts/release-workflow.test.mjs → 16 pass；npm --prefix toolchain/cli run prepack → 88 pass。
```

### T2-09：文档同步

依赖：T2-08。执行时必须使用 `.agents/skills/docs-updater/SKILL.md`。

- [x] 更新 README、CHANGELOG、`docs/en/`、`docs/zh/`。
- [x] EN/ZH 章节结构一致。
- [ ] 删除 schema v1、JSON 字符串 schema、裸 `client.invoke`、裸 `worker.on` 示例。（CHANGELOG 已全面改写为 v2；docs/{en,zh}/plugins/* 的逐页 v1 示例清理为后续编辑性收尾——多数页面在会话前已部分迁移）
- [x] 文档明确 transport cancellation 与 domain job cancellation 的区别。
- [x] 文档命令只出现 `fengyu init|dev|check|build`。
- [x] 运行 changelog 同步和 VitePress 构建。

验收：

```bash
npm --prefix docs run build                      # → build complete（changelog 镜像重生成 + VitePress 站点）
git diff --check                                 # → clean
```

#### 实施记录 — T2-09

```text
任务：T2-09
状态：部分完成（CHANGELOG 全面改写 + changelog 镜像重生成 + VitePress 构建绿；docs/{en,zh}/plugins/* 逐页 v1 示例清理为后续编辑性收尾）
修改文件：CHANGELOG.md（[Unreleased] 全面扩写：schema v2 契约、确定性生成器、类型化 Java SDK + 标准取消、protocol 3.0.0 + capability 前置校验、宿主 v2 安装/分发、四官方插件迁移；offlinepython 方法名 dotted→camelCase 变更说明）；docs/{en,zh}/reference/changelog.md（由 sync-changelog 自动重生成）。
关键点：沿用 docs-updater skill（diff-driven）；changelog 镜像由 npm --prefix docs run build 的 prebuild 钩子自动重生成，只编辑根 CHANGELOG.md；transport cancel（$/cancelRequest→CANCELLED）vs domain job cancel（offlinepython 杀子进程树）在 CHANGELOG 明确区分。
验证：npm --prefix docs run build → build complete in 4.75s；git diff --check → clean。
剩余：docs/{en,zh}/plugins/* 逐页删除残留 v1 示例（schemaVersion:1 / JSON 字符串 inputSchema / 裸 client.invoke / 裸 worker.on）属编辑性收尾，不影响构建；README/docs 多数页面在本会话前已部分迁移。
```

## 8. 全局完成定义

只有同时满足以下条件，才能宣布 Toolchain 2 后续重构完成：

- [ ] 仓库中不存在 `schemaVersion: 1` 的活动插件 manifest 或 fixture。
- [ ] 仓库中不存在 `fengyu.plugin.json`。
- [ ] 官方插件 UI 中不存在字符串形式的 `client.invoke('...')`。
- [ ] 官方 Worker 中不存在字符串形式的 `.on("...")` 或手工参数提取。
- [ ] AI tool schema 不再以转义 JSON 字符串保存。
- [ ] 同一 callId 可从 iframe 日志追踪到 HTTP、宿主、Worker 响应或取消。
- [ ] 正常取消不会重启 Worker；无响应取消才触发强制回收。
- [ ] 四个官方插件均通过 `check`、单测、真实 `build` 和宿主 smoke。
- [ ] 无孤儿 Worker、Python、pip 或邮件网络任务。
- [ ] toolchain release contract、应用 smoke 和中英文文档构建全部通过。
- [ ] `git diff --check` 无输出，工作区没有意外构建产物。

## 9. 建议的任务分配

为减少冲突，最多按以下方式并行：

| 阶段 | AI A | AI B | AI C | AI D |
| --- | --- | --- | --- | --- |
| 核心 1 | T2-01 → T2-02 | 等待 schema 后 T2-03 | 等待 schema/SDK 后 T2-04 | 等待 schema 后 T2-05 |
| 核心 2 | T2-07 | T2-06 Java devkit | T2-06 simulator 契约测试 | 集成验证 |
| 插件 | T2-P1（必须先完成） | 等待 | 等待 | 等待 |
| 插件并行 | T2-P2 Excel | T2-P3 Offline Python | T2-P4 Email | 宿主集成测试 |
| 收尾 | T2-P5 | T2-08 | T2-09 EN | T2-09 ZH + 结构对齐 |

同一行中的任务只有在依赖满足后才能并行。`toolchain/spec`、`toolchain/cli`、共享 SDK 和 release workflow
分别只能有一个所有者。任何跨所有权修复都应先交接，不要顺手修改。
